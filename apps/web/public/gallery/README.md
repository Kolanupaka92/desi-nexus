# The gallery lives here.

Drop image files in this directory and describe them in `apps/web/content/gallery.ts`.
That file has the full instructions, including the rules about permission and
attribution. Until it has entries the gallery section does not render at all.
