{
  "name": "@desi-nexus/api",
  "version": "0.1.0",
  "private": true,
  "description": "DESI-NEXUS marketplace API: gig matching, cultural taxonomy and Stripe Connect escrow.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.23.1",
    "eslint": "^10.10.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.70.0"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "pg": "^8.23.0",
    "stripe": "^22.6.2"
  }
}
